BrokenAuth
Cookie Spoofing
в куке хранится роль и если роль админ мы получаем панель админа
h.Role, _ = r.Cookie("role")
if strings.ToLower(h.Role.Value) == "admin"
ipшники тоже проверяются нужно перебрать
for _, host := range []string{"127.0.0.1", "localhost"} {
	if host == strings.Split(h.ClientIP, ":")[0] {
    	fmt.Fprintln(w, html_AdminDashboard())
	}
}
